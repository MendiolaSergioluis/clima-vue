import type {Busqueda} from "@/helpers/Types";

export default function useClima() {

    const obtenerClima = (busqueda: Busqueda) => {
        // Consultar API desde aquí
    }

    return {
        obtenerClima,
    }
}